<form method="post" action="{{ path('admin_culture_category_delete', {'id': culture_category.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ culture_category.id) }}">
    <button class="btn">Delete</button>
</form>
