{% extends 'base.html.twig' %}

{% block title %}CultureCategory{% endblock %}

{% block body %}
    <h1>CultureCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ culture_category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ culture_category.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_culture_category_index') }}">back to list</a>

    <a href="{{ path('admin_culture_category_edit', {'id': culture_category.id}) }}">edit</a>

    {{ include('admin/culture_category/_delete_form.html.twig') }}
{% endblock %}
